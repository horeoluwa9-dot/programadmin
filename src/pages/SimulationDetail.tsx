import { useState } from "react";import { useParams } from "react-router-dom";import { PageHeader } from "@/components/layout/PageHeader";import { Button } from "@/components/ui/button";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Dialog,DialogContent,DialogFooter,DialogHeader,DialogTitle } from "@/components/ui/dialog";import { Badge } from "@/components/ui/badge";import { simulations } from "@/data/mock";import { toast } from "sonner";export default function SimulationDetail(){const {id}=useParams();const [open,setOpen]=useState(false);const s=simulations.find(x=>x.title.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")===id)||simulations[2];return <><PageHeader title={s.title} subtitle="Simulation dashboard and launch settings" actions={<><Button variant="outline" onClick={()=>toast.success("Scenario editor opened")}>Edit Scenario</Button><Button variant="outline" onClick={()=>toast.success("Participants notified")}>Notify Participants</Button><Button onClick={()=>setOpen(true)}>Launch Simulation</Button></>}/><div className="grid gap-4 lg:grid-cols-3"><Card><CardHeader><CardTitle className="text-base">Overview</CardTitle></CardHeader><CardContent className="space-y-2 text-sm"><p>{s.scenario}</p><Badge className="capitalize">{s.difficulty}</Badge><p>Enrolled students: 62</p><p>Countdown: 2 days</p></CardContent></Card><Card className="lg:col-span-2"><CardHeader><CardTitle className="text-base">Scenario briefing</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">Teams will negotiate acquisition terms, regulatory approvals, capital allocation, and post-merger integration risks across African markets.</CardContent></Card><Card className="lg:col-span-3"><CardHeader><CardTitle className="text-base">Launch settings</CardTitle></CardHeader><CardContent className="grid gap-3 md:grid-cols-4">{["Teams: 12","Duration: 3h","Judging: Faculty","Mode: Live"].map(x=><div key={x} className="rounded-md border p-3 text-sm">{x}</div>)}</CardContent></Card></div><Dialog open={open} onOpenChange={setOpen}><DialogContent><DialogHeader><DialogTitle>Launch Simulation</DialogTitle></DialogHeader><p className="text-sm text-muted-foreground">Confirm launch for 62 enrolled students.</p><DialogFooter><Button variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={()=>{toast.success("Simulation launched");setOpen(false)}}>Confirm launch</Button></DialogFooter></DialogContent></Dialog></>}
