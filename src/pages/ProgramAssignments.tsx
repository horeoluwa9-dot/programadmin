import { PageHeader } from "@/components/layout/PageHeader";import { Button } from "@/components/ui/button";import { Card,CardContent } from "@/components/ui/card";import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table";import { students } from "@/data/mock";import { toast } from "sonner";export default function ProgramAssignments(){const missing=students.slice(0,12);return <><PageHeader title="Customer Discovery — Missing Assignments" subtitle="Filtered assignment view: status=missing" actions={<><Button variant="outline" onClick={()=>toast.success("CSV downloaded")}>Download CSV</Button><Button onClick={()=>toast.success("Bulk reminder sent")}>Bulk notify</Button></>}/><Card><CardContent className="p-0"><Table><TableHeader><TableRow><TableHead>Student</TableHead><TableHead>Assignment</TableHead><TableHead>Deadline</TableHead><TableHead>Action</TableHead></TableRow></TableHeader><TableBody>{missing.map((s,i)=><TableRow key={s.id}><TableCell>{s.name}</TableCell><TableCell>Customer Discovery Field Notes</TableCell><TableCell>Overdue by {i+1} days</TableCell><TableCell><Button size="sm" variant="outline" onClick={()=>toast.success(`Reminder sent to ${s.name}`)}>Send reminder</Button></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></>}
